* Chat is a bit too rudimentary

* Contact details can't be modified at all from within the system (last
  minute add-on features...) and do need it

* _Really_ the system should keep old copies of documents rather than overwrite
  the data when you update a document
