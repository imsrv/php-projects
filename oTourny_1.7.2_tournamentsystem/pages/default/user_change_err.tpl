Affiliation:
 <template name="AFF_MAX">
  Affiliation must be Less than 50 Characters.
 </template name="AFF_MAX">

Aim:
 <template name="AIM_MAX">
  AIM must be Less than 50 Characters.
 </template name="AIM_MAX">

Email:
 <template name="EMAIL_FORMAT">
  Email is not a Valid format.
 </template name="EMAIL_FORMAT">
 <template name="EMAIL_DUB_AUSER">
  Email is already in Use or is awaiting Authorization.
 </template name="EMAIL_DUB_AUSER">

ICQ:
 <template name="ICQ_MAX">
  ICQ must be Less than 20 Characters.
 </template name="ICQ_MAX">

Location:
 <template name="LOC_MAX">
  Location must be Less than 50 Characters.
 </template name="LOC_MAX">

MSN:
 <template name="MSN_MAX">
  MSN must be Less than 150 Characters.
 </template name="MSN_MAX">

Name:
 <template name="NAME_DUB_USER">
  User Name already in use.
 </template name="NAME_DUB_USER">
 <template name="NAME_DUB_AUSER">
  User Name already in reserve for possible User. Please wait a few days and try again.
 </template name="NAME_DUB_AUSER">
 <template name="NAME_BOUNDS">
  Your Name must be more than 3 and less than 50 characters.
 </template name="NAME_BOUNDS">
 <template name="NAME_PASS">
  User Name can not be your password.
 </template name="NAME_PASS">

Password:
 <template name="PASS_BOUNDS">
  Password must be longer than 5 characters and less than 50 characters.
 </template name="PASS_BOUNDS">
 <template name="PASS_PASS">
  Password can not be "password". You are asking to be hacked.
 </template name="PASS_PASS">
 <template name="PASS_EMAIL">
  Password can not be your email.
 </template name="PASS_EMAIL">
 <template name="PASS_NAME">
  Password can not be your Name.
 </template name="PASS_NAME">

Real Name:
 <template name="REAL_MAX">
  Real Name must be Less than 50 Characters.
 </template name="REAL_MAX">

Tag:
 <template name="TAG_DUB_TAG">
  Tag already in use by another team.
 </template name="TAG_DUB_TAG">
 <template name="TAG_DUB_TEAM">
  Tag already is a Team's Name.
 </template name="TAG_DUB_TEAM">
 <template name="TAG_DUB_USER">
  Tag cannot be a User's Name.
 </template name="TAG_DUB_USER">
 <template name="TAG_MAX">
  Tag has a 7 character max.
 </template name="TAG_MAX">

Webpage:
 <template name="WEB_MAX">
  Webpage must be Less than 150 Characters.
 </template name="WEB_MAX">

Time Format:
 <template name="TIME_MAX">
  Time Format must be Less than 150 Characters.
 </template name="TIME_MAX">

Time:
 <template name="TIME_BAD">
  Time given is in an invalid format.
 </template name="TIME_BAD">